// Frontend-only config: set the API base URL when the UI is hosted separately (e.g., Vercel).
// Example for Vercel: window.API_BASE = "https://your-backend.onrender.com";
// Default (empty) means "same origin" — works for local dev with `npm start`.
window.API_BASE = window.API_BASE || "";
